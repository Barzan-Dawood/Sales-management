# دليل النسخ الاحتياطي التلقائي

## نظرة عامة

تم إضافة ميزة **النسخ الاحتياطي التلقائي** إلى نظام تجارتي. هذه الميزة تقوم بإنشاء نسخ احتياطية تلقائية لقاعدة البيانات حسب الجدولة المحددة.

## المميزات

### ✅ ما تم إضافته:

1. **خدمة النسخ التلقائي** (`AutoBackupService`)
   - تعمل في الخلفية تلقائياً
   - تتحقق من الحاجة للنسخ كل ساعة
   - تدعم 4 فترات زمنية:
     - كل ساعة (hourly)
     - يومي (daily)
     - أسبوعي (weekly)
     - شهري (monthly)

2. **إعدادات سهلة**
   - تفعيل/تعطيل النسخ التلقائي
   - اختيار فترة النسخ
   - عرض معلومات آخر نسخة احتياطية

3. **تسجيل الحالة**
   - حفظ حالة آخر نسخة احتياطية (نجاح/فشل)
   - حفظ رسالة الخطأ إن وجدت
   - حفظ تاريخ آخر نسخة احتياطية

## كيفية الاستخدام

### 1. تفعيل النسخ التلقائي

1. افتح التطبيق
2. اذهب إلى **الإعدادات** → **إعدادات قاعدة البيانات**
3. اختر تبويب **الإعدادات**
4. فعّل **النسخ الاحتياطي التلقائي**
5. اختر **فترة النسخ** (كل ساعة / يومي / أسبوعي / شهري)
6. تأكد من تحديد **مسار النسخ الاحتياطي**

### 2. مراقبة النسخ التلقائي

- في تبويب **الإعدادات**، ستجد معلومات عن آخر نسخة احتياطية:
  - تاريخ آخر نسخة
  - حالة النسخة (نجاح/فشل)
  - رسالة الحالة

### 3. النسخ اليدوي

يمكنك أيضاً إنشاء نسخة احتياطية يدوياً في أي وقت من تبويب **النسخ الاحتياطي**.

## التفاصيل التقنية

### الملفات المضافة/المعدلة:

1. **`lib/src/services/auto_backup_service.dart`** (جديد)
   - خدمة النسخ التلقائي الرئيسية
   - تدير Timer للتحقق الدوري
   - تنفذ النسخ عند الحاجة

2. **`lib/main.dart`** (معدل)
   - تهيئة خدمة النسخ التلقائي عند بدء التطبيق

3. **`lib/src/screens/database_settings_dialog.dart`** (معدل)
   - إضافة خيار "كل ساعة"
   - عرض معلومات آخر نسخة احتياطية
   - إعادة تشغيل الخدمة عند تغيير الإعدادات

### آلية العمل:

1. عند بدء التطبيق:
   - يتم تهيئة `AutoBackupService`
   - التحقق من تفعيل النسخ التلقائي
   - التحقق من الحاجة لإنشاء نسخة احتياطية

2. كل ساعة:
   - يتم التحقق من:
     - هل النسخ التلقائي مفعّل؟
     - هل حان وقت النسخ حسب الفترة المحددة؟
   - إذا نعم، يتم إنشاء النسخة الاحتياطية

3. عند تغيير الإعدادات:
   - يتم إعادة تشغيل الخدمة مع الإعدادات الجديدة

### الإعدادات المحفوظة:

- `auto_backup_enabled`: تفعيل/تعطيل النسخ التلقائي
- `auto_backup_frequency`: فترة النسخ (hourly/daily/weekly/monthly)
- `backup_path`: مسار حفظ النسخ الاحتياطية
- `last_auto_backup_time`: وقت آخر نسخة احتياطية (timestamp)
- `last_auto_backup_status`: حالة آخر نسخة (success/error/unknown)
- `last_auto_backup_message`: رسالة الحالة

## ملاحظات مهمة

### ⚠️ تحذيرات:

1. **مسار النسخ الاحتياطي:**
   - يجب تحديد مسار صحيح قبل تفعيل النسخ التلقائي
   - تأكد من وجود مساحة كافية في القرص

2. **الأداء:**
   - النسخ التلقائي يعمل في الخلفية
   - قد يستغرق بضع ثوانٍ حسب حجم قاعدة البيانات
   - لا يؤثر على استخدام التطبيق

3. **النسخ القديمة:**
   - يتم الاحتفاظ بآخر 10 نسخ احتياطية فقط
   - يتم حذف النسخ الأقدم تلقائياً

### 💡 نصائح:

1. **الفترة الموصى بها:**
   - للمحلات الصغيرة: يومي أو أسبوعي
   - للمحلات الكبيرة: يومي أو كل ساعة
   - للمحلات المتوسطة: أسبوعي

2. **النسخ اليدوي:**
   - يُنصح بإنشاء نسخة احتياطية يدوياً قبل:
     - تحديثات كبيرة
     - عمليات حذف مهمة
     - تغييرات كبيرة في البيانات

3. **التحقق من النسخ:**
   - راجع معلومات آخر نسخة احتياطية بانتظام
   - تأكد من نجاح النسخ التلقائي

## استكشاف الأخطاء

### المشكلة: النسخ التلقائي لا يعمل

**الحلول:**
1. تأكد من تفعيل النسخ التلقائي في الإعدادات
2. تأكد من تحديد مسار النسخ الاحتياطي
3. تحقق من وجود مساحة كافية في القرص
4. راجع رسالة الخطأ في معلومات آخر نسخة احتياطية

### المشكلة: رسالة خطأ في النسخ

**الحلول:**
1. تحقق من صحة مسار النسخ الاحتياطي
2. تأكد من وجود صلاحيات الكتابة في المسار
3. تحقق من مساحة القرص المتاحة
4. جرب إنشاء نسخة يدوية للتحقق

## التطوير المستقبلي

### ميزات محتملة:

- [ ] إشعارات عند إنشاء نسخة احتياطية
- [ ] نسخ احتياطي سحابي (Cloud Backup)
- [ ] جدولة متقدمة (أيام محددة، أوقات محددة)
- [ ] ضغط النسخ الاحتياطية
- [ ] تشفير النسخ الاحتياطية

---

*تم إضافة هذه الميزة في: 2025*

