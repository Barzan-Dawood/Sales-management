# 🪟 دليل بناء نسخة Windows للتثبيت على أجهزة أخرى

<div dir="rtl">

## 📋 نظرة عامة

هذا الدليل يشرح كيفية بناء نسخة Windows من تطبيق **تجارتي** وإنشاء ملف تثبيت يمكن نقله وتثبيته على أي جهاز Windows آخر.

---

## ✅ المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

1. **Flutter SDK** (3.3.0 أو أحدث)
   - تحميل من: https://flutter.dev/docs/get-started/install/windows
   - إضافة Flutter إلى PATH

2. **Visual Studio 2022** (Community أو Professional)
   - تحميل من: https://visualstudio.microsoft.com/downloads/
   - أثناء التثبيت، اختر:
     - ✅ Desktop development with C++
     - ✅ Windows 10/11 SDK (أحدث إصدار)

3. **Git** (اختياري)
   - تحميل من: https://git-scm.com/downloads

---

## 🚀 الطريقة 1: بناء نسخة Release بسيطة (EXE)

### الخطوة 1: التحقق من الإعداد

افتح PowerShell أو Command Prompt في مجلد المشروع وقم بتشغيل:

```bash
flutter doctor
```

تأكد من أن جميع المتطلبات مُثبتة بشكل صحيح.

### الخطوة 2: تحديث التبعيات

```bash
flutter pub get
```

### الخطوة 3: بناء نسخة Release

```bash
flutter build windows --release
```

سيتم إنشاء الملفات في:
```
build\windows\x64\runner\Release\
```

### الخطوة 4: نسخ الملفات للتوزيع

بعد البناء، ستحتاج إلى نسخ المجلد بالكامل. الملفات المطلوبة:

- `tijarati.exe` - الملف التنفيذي الرئيسي
- `data\` - مجلد يحتوي على جميع الأصول والملفات المطلوبة
- جميع ملفات `.dll` في نفس المجلد

**ملاحظة:** يمكنك نسخ مجلد `Release` بالكامل ونقله إلى أي جهاز Windows آخر.

---

## 📦 الطريقة 2: إنشاء ملف ZIP للتوزيع

### استخدام السكريبت المرفق

قم بتشغيل السكريبت `build_windows_release.ps1` (موجود في المشروع):

```powershell
.\build_windows_release.ps1
```

أو يدوياً:

### الخطوة 1: بناء نسخة Release

```bash
flutter build windows --release
```

### الخطوة 2: إنشاء مجلد للتوزيع

```powershell
# إنشاء مجلد جديد
New-Item -ItemType Directory -Path "release\windows" -Force

# نسخ الملفات
Copy-Item -Path "build\windows\x64\runner\Release\*" -Destination "release\windows\" -Recurse -Force
```

### الخطوة 3: ضغط الملفات

```powershell
# ضغط المجلد
Compress-Archive -Path "release\windows" -DestinationPath "release\tijarati-windows-v1.0.0.zip" -Force
```

الآن يمكنك مشاركة ملف ZIP مع المستخدمين الآخرين.

---

## 🎁 الطريقة 3: إنشاء ملف تثبيت (Installer) باستخدام Inno Setup

### الخطوة 1: تحميل وتثبيت Inno Setup

- تحميل من: https://jrsoftware.org/isdl.php
- تثبيت البرنامج

### الخطوة 2: بناء نسخة Release

```bash
flutter build windows --release
```

### الخطوة 3: إنشاء ملف Script لـ Inno Setup

استخدم ملف `tijarati_installer.iss` المرفق في المشروع (أو أنشئه يدوياً).

### الخطوة 4: تجميع ملف التثبيت

1. افتح Inno Setup Compiler
2. افتح ملف `tijarati_installer.iss`
3. اضغط **Build** > **Compile**
4. سيتم إنشاء ملف `.exe` في مجلد `Output`

---

## 🔧 الطريقة 4: إنشاء ملف MSIX (موصى به للتوزيع الحديث)

### المتطلبات الإضافية

- **Windows SDK** (أحدث إصدار)
- **MSIX Packaging Tool** (اختياري)

### الخطوة 1: بناء نسخة Release

```bash
flutter build windows --release
```

### الخطوة 2: استخدام Flutter لإنشاء MSIX

```bash
flutter build windows --release
flutter pub global activate msix
flutter pub global run msix:create
```

---

## 📝 تفاصيل إضافية

### معلومات الإصدار

يمكنك تحديث رقم الإصدار في `pubspec.yaml`:

```yaml
version: 1.0.0+1
```

- الرقم الأول (1.0.0) = رقم الإصدار
- الرقم الثاني (+1) = رقم البناء

### تخصيص اسم التطبيق

في `windows/runner/main.cpp`، يمكنك تغيير اسم النافذة:

```cpp
if (!window.Create(L"تجارتي", origin, size)) {
```

### تخصيص الأيقونة

1. استبدل `windows/runner/resources/app_icon.ico` بأيقونتك
2. يجب أن يكون الملف بصيغة `.ico` بحجم 256x256 على الأقل

---

## 🚚 توزيع النسخة على أجهزة أخرى

### الطريقة 1: عبر USB أو شبكة محلية

1. انسخ ملف ZIP أو EXE إلى USB
2. انقله إلى الجهاز الآخر
3. استخرج الملفات (إذا كان ZIP)
4. شغّل `tijarati.exe`

### الطريقة 2: عبر الإنترنت

1. ارفع الملف إلى:
   - Google Drive
   - Dropbox
   - OneDrive
   - أو أي خدمة تخزين سحابي
2. شارك الرابط مع المستخدمين

### الطريقة 3: عبر موقع ويب

1. ارفع الملف إلى خادم الويب
2. أنشئ صفحة تحميل بسيطة

---

## ⚠️ ملاحظات مهمة

### 1. Visual C++ Redistributable

على الجهاز الآخر، قد تحتاج إلى تثبيت:
- **Visual C++ Redistributable for Visual Studio 2022**
- تحميل من: https://aka.ms/vs/17/release/vc_redist.x64.exe

### 2. Windows Defender

قد يظهر تحذير "Windows protected your PC" لأن التطبيق غير موقّع. هذا طبيعي:
- اضغط **More info**
- ثم **Run anyway**

### 3. التوقيع الرقمي (اختياري)

لإزالة التحذيرات، يمكنك:
- شراء شهادة توقيع من شركة معتمدة
- أو استخدام شهادة من Microsoft Store

---

## 🐛 استكشاف الأخطاء

### المشكلة: خطأ في البناء

```bash
# تنظيف البناء السابق
flutter clean

# إعادة البناء
flutter pub get
flutter build windows --release
```

### المشكلة: ملفات DLL مفقودة

تأكد من نسخ جميع ملفات `.dll` من مجلد `Release` مع `tijarati.exe`.

### المشكلة: التطبيق لا يعمل على جهاز آخر

1. تأكد من تثبيت Visual C++ Redistributable
2. تأكد من أن Windows محدث
3. تحقق من أن جميع الملفات موجودة في نفس المجلد

---

## 📊 حجم الملفات المتوقع

- **EXE فقط:** ~50-80 MB
- **مجلد Release كامل:** ~150-200 MB
- **ملف ZIP مضغوط:** ~80-120 MB
- **ملف Installer:** ~100-150 MB

---

## ✅ قائمة التحقق النهائية

قبل توزيع النسخة، تأكد من:

- [ ] التطبيق يعمل بشكل صحيح على جهازك
- [ ] جميع الملفات موجودة في مجلد Release
- [ ] تم اختبار التطبيق على جهاز Windows آخر (إن أمكن)
- [ ] رقم الإصدار محدث في `pubspec.yaml`
- [ ] تم إنشاء ملف README بسيط مع تعليمات التثبيت
- [ ] تم ضغط الملفات (إذا كان ZIP)

---

## 📞 الدعم

إذا واجهت أي مشاكل:

- **الهاتف:** 07866744144
- **البريد الإلكتروني:** barzan.dawood.dev@gmail.com

---

**آخر تحديث:** يناير 2026

</div>

