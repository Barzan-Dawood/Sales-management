# دليل إدارة رموز الدخول

## نظرة عامة

نظراً لأن التطبيق غير متصل بالإنترنت، لا يمكن استخدام البريد الإلكتروني لإعادة تعيين كلمات المرور. لكن هناك عدة طرق لتغيير رموز الدخول:

---

## الطريقة 1: التغيير المباشر من التطبيق (الأسهل) ✅

### للمدير:
1. سجل الدخول بحساب المدير (`manager` / `admin123`)
2. اذهب إلى **إدارة المستخدمين**
3. ستجد حقول لتعديل:
   - اسم المستخدم للمشرف
   - كلمة المرور للمشرف
   - اسم المستخدم للموظف
   - كلمة المرور للموظف
4. أدخل البيانات الجديدة واضغط **حفظ**

### ملاحظة مهمة:
- **المدير لا يمكنه تغيير كلمة مروره من هذه الشاشة** (لأسباب أمنية)
- لتغيير كلمة مرور المدير، استخدم زر **إعادة ضبط** ثم غيّرها يدوياً

---

## الطريقة 2: التصدير والاستيراد (لنقل الإعدادات بين الأجهزة) 📤📥

### تصدير بيانات المستخدمين:
1. سجل الدخول بحساب المدير
2. اذهب إلى **إدارة المستخدمين**
3. اضغط على زر **تصدير بيانات المستخدمين** (أخضر)
4. سيتم حفظ ملف JSON في مجلد المستندات
5. يمكنك نسخ هذا الملف ونقله إلى جهاز آخر عبر:
   - USB
   - Bluetooth
   - Email (إذا كان متاحاً)
   - أي طريقة نقل ملفات

### استيراد بيانات المستخدمين:
1. سجل الدخول بحساب المدير
2. اذهب إلى **إدارة المستخدمين**
3. اضغط على زر **استيراد بيانات المستخدمين** (أزرق)
4. اختر ملف JSON الذي تم تصديره
5. سيتم تحديث أسماء المستخدمين تلقائياً

### ملاحظات:
- **كلمات المرور لا يتم تصديرها** لأسباب أمنية
- بعد الاستيراد، يجب تغيير كلمات المرور يدوياً
- الملف يحتوي على: أسماء المستخدمين، الأسماء، رموز الموظفين

---

## الطريقة 3: إعادة الضبط إلى القيم الافتراضية 🔄

إذا نسيت جميع كلمات المرور:

1. سجل الدخول بحساب المدير (إذا كنت تعرف كلمة المرور)
2. اذهب إلى **إدارة المستخدمين**
3. اضغط على زر **إعادة ضبط أسماء المستخدمين وكلمات المرور** (برتقالي)
4. سيتم إعادة تعيين جميع الحسابات إلى:
   - **المدير**: `manager` / `admin123`
   - **المشرف**: `supervisor` / `super123`
   - **الموظف**: `employee` / `emp123`

### تحذير:
⚠️ **هذه العملية ستحذف جميع التغييرات السابقة!**

---

## الطريقة 4: التعديل المباشر في قاعدة البيانات (للمستخدمين المتقدمين) ⚙️

إذا كنت تعرف كيفية التعامل مع قواعد البيانات:

1. افتح ملف قاعدة البيانات: `pos_office.db`
2. استخدم أداة مثل **DB Browser for SQLite**
3. ابحث عن جدول `users`
4. عدّل الحقول:
   - `username`: اسم المستخدم الجديد
   - `password`: كلمة المرور مشفرة بـ SHA256

### تحذير:
⚠️ **تأكد من تشفير كلمة المرور بـ SHA256 قبل الحفظ!**

---

## الحلول المقترحة للتطبيق غير المتصل بالإنترنت

### ✅ الحلول المتاحة حالياً:
1. **التغيير المباشر** - من داخل التطبيق (للمشرف والموظف)
2. **التصدير/الاستيراد** - لنقل الإعدادات بين الأجهزة
3. **إعادة الضبط** - للعودة للقيم الافتراضية

### 💡 حلول مستقبلية مقترحة:
1. **QR Code** - لاستيراد الإعدادات بسرعة
2. **ملف نصي بسيط** - يمكن تحريره يدوياً
3. **نسخ احتياطي تلقائي** - مع إمكانية استعادة كلمات المرور

---

## نصائح أمنية 🔒

1. **لا تشارك كلمات المرور** مع أشخاص غير مصرح لهم
2. **غيّر كلمات المرور الافتراضية** فور تثبيت التطبيق
3. **احتفظ بنسخة احتياطية** من ملف التصدير في مكان آمن
4. **استخدم كلمات مرور قوية** (8 أحرف على الأقل، مزيج من أرقام وحروف)
5. **لا تحفظ كلمات المرور** في ملفات نصية غير مشفرة

---

## الأسئلة الشائعة

### س: كيف أغير كلمة مرور المدير؟
**ج:** استخدم زر "إعادة ضبط" ثم غيّرها يدوياً من شاشة إدارة المستخدمين.

### س: هل يمكن استعادة كلمة المرور المنسية؟
**ج:** نعم، استخدم زر "إعادة ضبط" للعودة للقيم الافتراضية.

### س: هل يمكن نقل الإعدادات بين الأجهزة؟
**ج:** نعم، استخدم ميزة التصدير/الاستيراد.

### س: لماذا لا يتم تصدير كلمات المرور؟
**ج:** لأسباب أمنية. كلمات المرور مشفرة ولا يمكن استخراجها.

---

## الدعم

إذا واجهت أي مشاكل، تحقق من:
1. رسائل الخطأ في وحدة التحكم (Console)
2. ملفات السجل (Logs)
3. تأكد من أنك تستخدم أحدث إصدار من التطبيق

---

**آخر تحديث:** 2024

